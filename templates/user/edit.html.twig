{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
<div class="containe d-flex justify-content-center align-items-center flex-column">
    <h1 class="display-4 my-4">Edit User</h1>

    {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}
    <div class="d-flex align-items-start col-2">

        
        <a href="{{ path('app_user_index') }}" class="btn btn-secondary m-1">back to list</a>
        
        {{ include('user/_delete_form.html.twig') }}
    </div>
{% endblock %}
